[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cipgd"
version = "0.1.0"
description = "Semantic and topological analysis workflow for CIP Global Dialogues data."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
